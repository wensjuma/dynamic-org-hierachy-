import { OrgachartDirective } from './orgachart.directive';

describe('OrgachartDirective', () => {
  it('should create an instance', () => {
    const directive = new OrgachartDirective();
    expect(directive).toBeTruthy();
  });
});
